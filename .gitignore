# Compiled binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# PII detection patterns (real values — must NOT be committed)
.piipatterns

# Plan files (editor scratch)
.zcode/

# Project-local GOPATH (mise-installed tools: gopls, dlv, staticcheck)
/.gopath/

# Go workspace files (local only)
/go.work
/go.work.sum

# Linux driver reference source (cloned separately, not tracked)
/references/linux-driver/
cmd/qmidial/qmidial_out.txt
cmd/qmidial/qmidial_done.txt
/qmidial_out.txt
/qmidial_done.txt
/socks5_out.txt
/socks5_err.txt


# Environment file with hardware test secrets (phone numbers, etc.)
.env

# Desktop client (Wails) build artifacts
/desktop/desktop
/desktop/build/bin/
/desktop/frontend/node_modules/
/desktop/frontend/dist/